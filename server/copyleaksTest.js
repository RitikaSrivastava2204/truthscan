// const { submitScan } = require('./copyleaks'); // adjust path if needed

// async function testCopyleaks() {
//   const response = await submitScan("This is a test to check if Copyleaks is working.");
//   console.log("Copyleaks test response:", response);
// }

// testCopyleaks();
